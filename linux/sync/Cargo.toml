[package]
name = "linux-sync"
version = "0.1.0"
edition = "2024"
description = "Linux kernel synchronization primitives"

[dependencies]
linux-core = { path = "../core" }
libos-sync = { path = "../../libos/sync" }
