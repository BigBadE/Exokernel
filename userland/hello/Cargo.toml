[package]
name = "hello"
version = "0.1.0"
edition = "2024"
description = "Hello world test application for the Exokernel"

[dependencies]
libexo = { path = "../../kernel/lib" }
exo-shared = { path = "../../kernel/shared" }
